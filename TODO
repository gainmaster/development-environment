Rename base-image to machine
Make so build script use machine/<folder> folder name as tag
Make a generic macine test script
Enable configuration of CoreOS machine
Toolbox folder?
Add ENVIRONMENT variable


REMOVES ABANDONED DOCKER IMAGES
docker images | grep '<none>' | awk 'NR =3 {print $3}' | xargs -l docker rmi